import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.caivratech.speedmathcoach',
  appName: 'SpeedMath Coach',
  webDir: 'dist'
};

export default config;
